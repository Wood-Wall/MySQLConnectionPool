
#include <iostream>
using namespace std;
#include"Connection.h"

int main()
{
	//Connection conn;
	//char sql[1024] = { 0 };
	//sprintf_s(sql, "insert into user(name,age,sex) values('%s',%d,%d)",
	//	"zhang san", 20, 0);
	//if(conn.connect("127.0.0.1", 3306, "root", "password", "chat"))
	//	conn.update(sql);
}